[project]
name = "healthcost-shared"
version = "0.1.0"
description = "Shared contracts for Healthcare Cost Predictor services"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

